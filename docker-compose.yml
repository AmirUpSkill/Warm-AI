version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: warmai-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: warmai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Backend and Frontend are commented out until Dockerfiles are created
  # backend:
  #   build: ./backend
  #   env_file: .env
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     - db

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend

volumes:
  postgres_data: